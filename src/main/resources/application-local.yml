# 本地开发环境。激活方式：-Dspring.profiles.active=local 或 SPRING_PROFILES_ACTIVE=local
spring:
  datasource:
    url: jdbc:mysql://170.106.105.131:3306/aimate?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: admin
    password: xp43520!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 8
      minimum-idle: 2
      connection-timeout: 30000
      max-lifetime: 600000
      idle-timeout: 300000

agent:
  llm:
    primary:
      name: deepseek-chat
      base-url: https://api.deepseek.com/v1
      api-key: sk-458d2a115a034ec289b1bfe06f0beb6e
      model: deepseek-reasoner
      timeout-seconds: 120
    fallback:
      name: gpt-4o
      base-url: https://api.openai.com/v1
      api-key: ${PRIMARY_LLM_API_KEY:sk-placeholder}
      model: gpt-4o
      timeout-seconds: 120
  embedding:
    base-url: http://175.27.255.29:11434/v1
    api-key: ollama
    model: bge-m3
    dimensions: 1024
    timeout-seconds: 30
  script:
    docker:
      enabled: true
      idle-minutes: 30
      image: debian:bookworm-slim
      memory-limit: 4g
      cpu-limit: 2
      read-only-rootfs: false
      run-container-cmd-timeout-seconds: 300
      run-container-cmd-idle-timeout-seconds: 90
      script-timeout-seconds: 300
      script-idle-timeout-seconds: 90
  milvus:
    enabled: true
    host: 175.27.255.29
    port: 19530
    collection-name: agent_memories_v2
    vector-dimensions: 1024
    # 连接超时(ms)。若 DEADLINE_EXCEEDED，说明本机无法访问 host:port，请检查网络/防火墙/FRP(需 type=tcp)
    connect-timeout-ms: 30000

app:
  jwt:
    secret: ${JWT_SECRET:OpenForgeX-super-secret-key-change-me-in-prod-2026}

logging:
  level:
    com.openforge.aimate: DEBUG
    org.hibernate.SQL: WARN
