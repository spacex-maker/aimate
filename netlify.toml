# Netlify 仅部署前端（frontend/），后端需单独部署。
# 在 Netlify 后台设置环境变量：VITE_API_BASE = 后端地址，如 https://api.example.com（不要末尾斜杠）

[build]
  base     = "frontend"
  command  = "npm run build"
  publish  = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA：所有路径回退到 index.html，由 React Router 处理
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
